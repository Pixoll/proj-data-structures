#pragma once

#include <iostream>
#include <string>

using namespace std;

class lempel_ziv {
public:
    lempel_ziv() = default;

    ~lempel_ziv() = default;

    string compress(const string &text) {
        return "";
    }

    string decompress(const string &compressed) {
        return "";
    }
};
